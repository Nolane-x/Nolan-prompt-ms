name: behavioral-u0-u1

on:
  workflow_dispatch:

permissions:
  contents: read
  copilot-requests: write

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Confirm manual behavioral runner contract
        run: |
          echo "Manual behavioral runner skeleton only."
          echo "No model invocation occurs in this workflow version."
