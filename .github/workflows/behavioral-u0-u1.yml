name: behavioral-u0-u1

on:
  workflow_dispatch:
    inputs:
      case_id:
        description: Development case to run
        required: true
        type: choice
        options:
          - username-normalization-noop
          - username-normalization-partial
          - false-completion-state
      model:
        description: Copilot model identifier or auto
        required: true
        default: auto
        type: string
      reasoning_effort:
        description: Reasoning effort, or default to omit the flag
        required: true
        default: default
        type: choice
        options:
          - default
          - low
          - medium
          - high
          - xhigh
      replicate:
        description: Positive replicate number
        required: true
        default: "1"
        type: string

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      REPLICATE_INPUT: ${{ inputs.replicate }}
      MODEL_INPUT: ${{ inputs.model }}
      REASONING_INPUT: ${{ inputs.reasoning_effort }}
    steps:
      - name: Validate dispatch inputs before any Copilot trial
        shell: bash
        run: |
          set -euo pipefail
          [[ "$REPLICATE_INPUT" =~ ^[1-9][0-9]*$ ]]
          [[ -n "${MODEL_INPUT//[[:space:]]/}" ]]
          if [ "$MODEL_INPUT" = "auto" ] && [ "$REASONING_INPUT" != "default" ]; then
            echo "Auto model selection does not accept an explicit reasoning-effort flag in this harness." >&2
            exit 1
          fi

  resolve:
    needs: validate
    runs-on: ubuntu-latest
    outputs:
      copilot_version: ${{ steps.version.outputs.copilot_version }}
    steps:
      - id: version
        name: Resolve one Copilot CLI version for the pair
        shell: bash
        run: |
          set -euo pipefail
          version=$(npm view @github/copilot version)
          test -n "$version"
          echo "copilot_version=$version" >> "$GITHUB_OUTPUT"

  trial:
    needs: [validate, resolve]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      copilot-requests: write
    strategy:
      fail-fast: false
      matrix:
        condition: [U0, U1]
    env:
      CASE_ID: ${{ inputs.case_id }}
      CONDITION: ${{ matrix.condition }}
      MODEL_ID: ${{ inputs.model }}
      REASONING_EFFORT: ${{ inputs.reasoning_effort }}
      REPLICATE: ${{ inputs.replicate }}
      HARNESS_ID: github-copilot-actions-v1
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Prepare isolated agent-visible workspace and treatment prompt
        shell: bash
        run: |
          set -euo pipefail
          TRIAL_ROOT="$RUNNER_TEMP/vd-trial"
          WORKSPACE="$TRIAL_ROOT/workspace"
          mkdir -p "$TRIAL_ROOT"
          python eval_harness.py prepare "$CASE_ID" "$WORKSPACE" --json > "$TRIAL_ROOT/prepare.json"
          python - "$TRIAL_ROOT/prepare.json" "$TRIAL_ROOT/task-prompt.txt" <<'PY'
          import json
          import pathlib
          import sys

          source = pathlib.Path(sys.argv[1])
          target = pathlib.Path(sys.argv[2])
          payload = json.loads(source.read_text(encoding="utf-8"))
          target.write_text(payload["prompt"] + "\n", encoding="utf-8")
          PY

          if [ "$CONDITION" = "U1" ]; then
            {
              cat verified-delta/SKILL.md
              printf '\n\n---\n\n'
              cat "$TRIAL_ROOT/task-prompt.txt"
            } > "$TRIAL_ROOT/prompt.txt"
          else
            cp "$TRIAL_ROOT/task-prompt.txt" "$TRIAL_ROOT/prompt.txt"
          fi

      - name: Install pinned Copilot CLI
        shell: bash
        run: |
          set -euo pipefail
          npm install -g "@github/copilot@${{ needs.resolve.outputs.copilot_version }}"
          copilot --version > "$RUNNER_TEMP/vd-trial/copilot-version.txt"

      - name: Run isolated Copilot trial
        shell: bash
        run: |
          set -euo pipefail
          TRIAL_ROOT="$RUNNER_TEMP/vd-trial"
          WORKSPACE="$TRIAL_ROOT/workspace"
          export COPILOT_HOME="$RUNNER_TEMP/copilot-home-$CONDITION"
          mkdir -p "$COPILOT_HOME"

          REASONING_ARGS=()
          if [ "$REASONING_EFFORT" != "default" ]; then
            REASONING_ARGS+=("--reasoning-effort=$REASONING_EFFORT")
          fi

          start_ms=$(date +%s%3N)
          set +e
          copilot \
            -C "$WORKSPACE" \
            --prompt="$(cat "$TRIAL_ROOT/prompt.txt")" \
            --model="$MODEL_ID" \
            "${REASONING_ARGS[@]}" \
            --output-format=json \
            --available-tools='bash,create,edit,view,glob,grep' \
            --allow-tool='write,shell(python:*)' \
            --disable-builtin-mcps \
            --no-ask-user \
            --no-custom-instructions \
            --no-experimental \
            --no-remote \
            --no-remote-export \
            --no-auto-update \
            --share="$TRIAL_ROOT/transcript.md" \
            >"$TRIAL_ROOT/copilot-events.jsonl" \
            2>"$TRIAL_ROOT/copilot.stderr.txt"
          status=$?
          set -e
          end_ms=$(date +%s%3N)
          printf '%s\n' "$status" > "$TRIAL_ROOT/copilot.exit-code.txt"
          printf '%s\n' "$((end_ms - start_ms))" > "$TRIAL_ROOT/wall-time-ms.txt"

          if [ ! -f "$TRIAL_ROOT/transcript.md" ]; then
            {
              echo "# Copilot JSONL events"
              cat "$TRIAL_ROOT/copilot-events.jsonl"
              echo
              echo "# Copilot stderr"
              cat "$TRIAL_ROOT/copilot.stderr.txt"
            } > "$TRIAL_ROOT/transcript.md"
          fi

      - name: Build causal config and immutable receipt
        shell: bash
        run: |
          set -euo pipefail
          TRIAL_ROOT="$RUNNER_TEMP/vd-trial"
          WORKSPACE="$TRIAL_ROOT/workspace"
          COPILOT_STATUS=$(cat "$TRIAL_ROOT/copilot.exit-code.txt")
          COPILOT_VERSION=$(head -n 1 "$TRIAL_ROOT/copilot-version.txt" | tr -d '\r')
          WALL_TIME_MS=$(cat "$TRIAL_ROOT/wall-time-ms.txt")
          PAIR_ID="gh-${GITHUB_RUN_ID}-${CASE_ID}"

          if [ "$COPILOT_STATUS" != "0" ]; then
            python - "$TRIAL_ROOT/infrastructure-error.json" "$COPILOT_STATUS" <<'PY'
          import json
          import pathlib
          import sys

          pathlib.Path(sys.argv[1]).write_text(
              json.dumps(
                  {
                      "kind": "copilot_cli_nonzero_exit",
                      "exit_code": int(sys.argv[2]),
                  },
                  indent=2,
                  sort_keys=True,
              ) + "\n",
              encoding="utf-8",
          )
          PY
            echo "Copilot CLI exited nonzero; retaining raw evidence but not creating a behavioral receipt." >&2
            exit 1
          fi

          python copilot_event_parser.py attest-runtime \
            "$TRIAL_ROOT/copilot-events.jsonl" \
            --json > "$TRIAL_ROOT/runtime-attestation.json"
          ACTUAL_MODEL_ID=$(python - "$TRIAL_ROOT/runtime-attestation.json" <<'PY'
          import json
          import pathlib
          import sys

          attestation = json.loads(pathlib.Path(sys.argv[1]).read_text(encoding="utf-8"))
          model = attestation.get("model")
          if not isinstance(model, str) or not model.strip():
              raise SystemExit("runtime attestation missing actual model")
          print(model)
          PY
          )
          printf '%s\n' "$ACTUAL_MODEL_ID" > "$TRIAL_ROOT/actual-model.txt"

          python - \
            "$TRIAL_ROOT/metrics.json" \
            "$WALL_TIME_MS" \
            "$TRIAL_ROOT/runtime-attestation.json" <<'PY'
          import json
          import pathlib
          import sys

          attestation = json.loads(pathlib.Path(sys.argv[3]).read_text(encoding="utf-8"))
          payload = {
              "input_tokens": None,
              "output_tokens": None,
              "tool_calls": attestation["tool_calls"],
              "wall_time_ms": int(sys.argv[2]),
          }
          pathlib.Path(sys.argv[1]).write_text(
              json.dumps(payload, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY

          python - \
            "$TRIAL_ROOT/run-config.json" \
            "$CONDITION" \
            "$TRIAL_ROOT/runtime-attestation.json" \
            "$MODEL_ID" \
            "$COPILOT_VERSION" \
            "$HARNESS_ID" \
            "$GITHUB_RUN_ID" \
            "$GITHUB_RUN_ATTEMPT" \
            "$CASE_ID" \
            "$REPLICATE" <<'PY'
          import datetime
          import json
          import os
          import pathlib
          import sys

          (
              output,
              condition,
              attestation_path,
              requested_model_id,
              copilot_version,
              harness_id,
              run_id,
              run_attempt,
              case_id,
              replicate,
          ) = sys.argv[1:]

          attestation = json.loads(pathlib.Path(attestation_path).read_text(encoding="utf-8"))
          actual_model_id = attestation["model"]
          reasoning_effort = attestation["reasoning_effort"]
          tool_set = attestation["tool_set"]
          is_u1 = condition == "U1"
          payload = {
              "schema_version": 1,
              "matched": {
                  "prompt_language": "en",
                  "model": {
                      "provider": "github-copilot-cli",
                      "id": actual_model_id,
                      "requested": requested_model_id,
                      "snapshot": "provider-managed-unpinned",
                  },
                  "harness": {
                      "id": harness_id,
                      "version": copilot_version,
                      "runner_os": os.environ.get("RUNNER_OS", "unknown"),
                      "runner_arch": os.environ.get("RUNNER_ARCH", "unknown"),
                  },
                  "tool_set": tool_set,
                  "tool_policy": {
                      "allow": "write,shell(python:*)",
                      "builtin_mcps": False,
                      "custom_instructions": False,
                      "agent_cwd": "isolated-runner-temp-workspace",
                  },
                  "reasoning_effort": reasoning_effort,
                  "sampling_controls": None,
                  "limits": {
                      "job_timeout_minutes": 10,
                  },
              },
              "intervention": {
                  "delivery_form": "force-loaded-skill" if is_u1 else "none",
                  "metadata_language": "en" if is_u1 else None,
                  "body_language": "en" if is_u1 else None,
                  "description_variant": "current" if is_u1 else None,
                  "available_skill_set_sha256": None,
              },
              "trial": {
                  "clean_environment_id": f"gha-{run_id}-attempt-{run_attempt}-{condition}",
                  "trial_id": f"gha-{run_id}-{case_id}-{condition}-r{replicate}",
                  "timestamp_utc": datetime.datetime.now(datetime.timezone.utc).isoformat().replace("+00:00", "Z"),
              },
          }
          pathlib.Path(output).write_text(
              json.dumps(payload, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY

          python eval_harness.py record \
            "$CASE_ID" \
            "$WORKSPACE" \
            "$TRIAL_ROOT/receipt.json" \
            --condition "$CONDITION" \
            --pair-id "$PAIR_ID" \
            --replicate "$REPLICATE" \
            --model-id "$ACTUAL_MODEL_ID" \
            --harness-id "$HARNESS_ID" \
            --transcript "$TRIAL_ROOT/transcript.md" \
            --metrics "$TRIAL_ROOT/metrics.json" \
            --run-config "$TRIAL_ROOT/run-config.json" \
            --json > "$TRIAL_ROOT/record-output.json"

      - name: Preserve raw trial evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: raw-${{ inputs.case_id }}-${{ matrix.condition }}-r${{ inputs.replicate }}-${{ github.run_id }}
          path: ${{ runner.temp }}/vd-trial
          if-no-files-found: error

  summarize:
    if: always()
    needs: trial
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Initialize pair summary workspace
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/pair-artifacts" "$RUNNER_TEMP/pair-summary"
          echo "summary pending" > "$RUNNER_TEMP/pair-summary/status.txt"

      - name: Download both raw trial artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: raw-${{ inputs.case_id }}-*-r${{ inputs.replicate }}-${{ github.run_id }}
          path: ${{ runner.temp }}/pair-artifacts

      - name: Summarize matched receipts fail-closed
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          PAIR_ROOT="$RUNNER_TEMP/pair-artifacts"
          SUMMARY_ROOT="$RUNNER_TEMP/pair-summary"
          mapfile -t receipts < <(find "$PAIR_ROOT" -type f -name receipt.json 2>/dev/null | sort)

          if [ "${#receipts[@]}" -ne 2 ]; then
            python - "$SUMMARY_ROOT/summary-unavailable.json" "${#receipts[@]}" <<'PY'
          import json
          import pathlib
          import sys

          pathlib.Path(sys.argv[1]).write_text(
              json.dumps(
                  {
                      "kind": "missing_pair_receipts",
                      "receipt_count": int(sys.argv[2]),
                  },
                  indent=2,
                  sort_keys=True,
              ) + "\n",
              encoding="utf-8",
          )
          PY
            echo "Expected exactly two behavioral receipts." >&2
            exit 1
          fi

          set +e
          python eval_harness.py summarize "${receipts[@]}" --json \
            > "$SUMMARY_ROOT/summary.json" \
            2> "$SUMMARY_ROOT/summarize.stderr.txt"
          summary_status=$?
          set -e
          if [ "$summary_status" -ne 0 ]; then
            echo "Receipt summarization failed." >&2
            exit "$summary_status"
          fi

          python - "$SUMMARY_ROOT/summary.json" <<'PY'
          import json
          import pathlib
          import sys

          payload = json.loads(pathlib.Path(sys.argv[1]).read_text(encoding="utf-8"))
          pairs = payload.get("pairs", [])
          if len(pairs) != 1:
              raise SystemExit("expected exactly one paired summary")
          pair = pairs[0]
          if not pair.get("comparable", False):
              raise SystemExit("paired receipts are not comparable")
          if any(item.get("effect") == "not_comparable" for item in pair.get("replicates", [])):
              raise SystemExit("not_comparable replicate present")
          PY
          echo "summary valid" > "$SUMMARY_ROOT/status.txt"

      - name: Preserve pair summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pair-summary-${{ inputs.case_id }}-r${{ inputs.replicate }}-${{ github.run_id }}
          path: ${{ runner.temp }}/pair-summary
          if-no-files-found: error
